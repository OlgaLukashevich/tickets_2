package com.example.tickets_2.utils

import android.widget.Spinner
import android.widget.TextView
import com.example.tickets_2.models.Event

object UIUtils {
    fun setupSpinnerWithEvents(
        spinner: Spinner,
        events: List<Event>,
        eventInfoTextView: TextView
    ) {
        // Реализация этой функции может быть добавлена позже
    }
}
